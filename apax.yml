name: "testapp"
version: 0.0.0
type: app
targets:
  - "1500"
  - llvm
variables:
  APAX_BUILD_ARGS:
    - "--debug" # Generate debug information for target "1500"
installStrategy: strict
apaxVersion: 3.1.1
dependencies:
  "@ax/sdk": ^2311.0.3
